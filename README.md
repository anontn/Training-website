# 🏋️ Workout Tracker - Приложение для отслеживания тренировок

Современное веб-приложение для учета тренировок с красивым UI в стиле Apple.

## ✨ Особенности

- 📊 Отслеживание упражнений, подходов и весов
- 📈 Статистика прогресса с графиками
- 📅 История тренировок
- 💾 Автоматическое сохранение данных
- 🎨 Современный dark-mode дизайн
- 📱 Адаптивный интерфейс

## 🚀 Установка и запуск

### Требования

- Node.js (v18 или выше)
- npm или yarn
- MongoDB (для бэкенда)

### Установка зависимостей

```bash
npm install
```

### Настройка окружения

Создайте файл `.env` в корне проекта:

```env
REACT_APP_BACKEND_URL=http://localhost:8000
```

### Запуск приложения

```bash
# Режим разработки
npm run dev

# Сборка для продакшена
npm run build

# Предпросмотр продакшен сборки
npm run preview
```

Приложение откроется по адресу: `http://localhost:3000`

## 🗄️ Запуск бэкенда

Для работы приложения необходим запущенный бэкенд:

```bash
# Перейдите в папку с бэкендом
cd backend

# Установите зависимости Python
pip install -r requirements.txt

# Настройте .env файл с MongoDB URL
# MONGO_URL=mongodb://localhost:27017
# DB_NAME=workout_tracker
# CORS_ORIGINS=http://localhost:3000

# Запустите сервер
uvicorn server:app --reload --port 8000
```

## 📁 Структура проекта

```
workout-tracker/
├── src/
│   ├── components/       # Компоненты React
│   │   ├── ui/          # UI компоненты (Dialog, Select, etc.)
│   │   └── BottomNav.jsx
│   ├── pages/           # Страницы приложения
│   │   ├── AuthPage.jsx
│   │   ├── DashboardPage.jsx
│   │   ├── WorkoutPage.jsx
│   │   ├── HistoryPage.jsx
│   │   ├── StatsPage.jsx
│   │   └── ExercisesPage.jsx
│   ├── App.jsx          # Главный компонент
│   ├── App.css          # Стили приложения
│   ├── index.css        # Глобальные стили
│   └── index.jsx        # Точка входа
├── index.html           # HTML шаблон
├── package.json         # Зависимости
├── vite.config.js       # Конфигурация Vite
├── tailwind.config.js   # Конфигурация Tailwind
└── postcss.config.js    # Конфигурация PostCSS
```

## 🛠️ Технологии

- **React 18** - UI библиотека
- **Vite** - Сборщик и dev-сервер
- **Tailwind CSS** - Utility-first CSS фреймворк
- **React Router** - Маршрутизация
- **Axios** - HTTP клиент
- **Recharts** - Библиотека графиков
- **Radix UI** - Headless UI компоненты
- **Lucide React** - Иконки
- **Sonner** - Уведомления

## 📝 Основные функции

### Аутентификация
- Простой вход по имени
- Сохранение сессии в LocalStorage

### Тренировки
- Создание тренировок по датам
- Добавление упражнений
- Запись подходов (вес и повторения)
- Автосохранение

### История
- Просмотр всех тренировок
- Группировка по месяцам
- Удаление тренировок

### Статистика
- Графики прогресса по упражнениям
- Изменение веса и повторений
- Детальная статистика

### Упражнения
- Создание своих упражнений
- Удаление упражнений
- Быстрый доступ

## 🎨 Дизайн

Приложение использует современный минималистичный дизайн в темной теме с:
- Стеклянный эффект (glassmorphism)
- Плавные анимации
- Градиентные акценты
- Адаптивная типографика

## 📄 Лицензия

MIT

## 👨‍💻 Разработка

Для разработки используйте:

```bash
npm run dev
```

Приложение будет автоматически перезагружаться при изменении файлов.
