# ⚡ Быстрый старт - Развертывание на Render

## 🎯 Самый простой способ (5 минут)

### 1️⃣ Фронтенд на Vercel (бесплатно)

1. **Зайдите на [vercel.com](https://vercel.com)** и зарегистрируйтесь
2. Нажмите **"Add New" → "Project"**
3. Подключите ваш GitHub репозиторий
4. Настройки:
   - **Framework Preset**: Vite
   - **Build Command**: `npm run build`
   - **Output Directory**: `dist`
   - **Install Command**: `npm install`
   - ⚠️ **ВАЖНО**: Убедитесь, что файл `vercel.json` находится в корне проекта и закоммичен в Git!
5. **Environment Variables** (добавьте ПОСЛЕ деплоя бэкенда):
   ```
   VITE_BACKEND_URL=https://workout-tracker-api-ugge.onrender.com
   ```
6. Нажмите **"Deploy"**

✅ Фронтенд будет доступен на `https://your-project.vercel.app`

---

### 2️⃣ Бэкенд на Render (бесплатно)

1. **Зайдите на [render.com](https://render.com)** и зарегистрируйтесь
2. Нажмите **"New +" → "Web Service"**
3. Подключите ваш GitHub репозиторий
4. ⚠️ **ВАЖНО - Измените Language!**
   - Render может автоматически определить проект как **Docker** (если видите Dockerfile)
   - **Измените** поле **Language** с `Docker` на **`Python 3`**
   - После изменения Language появятся поля для Python команд
   
5. Настройки:
   - **Name**: `workout-tracker-api` (любое имя)
   - **Language**: `Python 3` ⚠️ **ОБЯЗАТЕЛЬНО измените с Docker!**
   - **Python Version** (если видите): `3.11.10` или оставьте по умолчанию
   - **Build Command**: `pip install -r requirements.txt`
   - **Start Command**: `uvicorn server_simple:app --host 0.0.0.0 --port $PORT`
   - **Plan**: Free (бесплатный)
   
   💡 **Совет**: В проекте есть файл `runtime.txt` с версией Python 3.11.10 - Render автоматически использует его
   
   ⚠️ **ВАЖНО**: 
   - **Build Command** и **Start Command** - это поля для команд (появятся после выбора Python 3)
   - **Included Paths** (если видите) - это для путей к файлам типа `requirements.txt`, `server_simple.py` - НЕ заполняйте командами!
   - Оставьте **Included Paths** пустым или используйте для указания файлов, изменения в которых должны триггерить деплой
   
6. Нажмите **"Create Web Service"**

✅ Бэкенд будет доступен на `https://your-app-name.onrender.com`

⚠️ **Важно**: При первом запросе может быть задержка ~30 секунд (холодный старт на бесплатном плане)

---

### 3️⃣ Обновите URL бэкенда во фронтенде

1. Вернитесь в Vercel → ваш проект → Settings → Environment Variables
2. Измените `VITE_BACKEND_URL` на URL вашего Render сервиса:
   ```
   VITE_BACKEND_URL=https://your-app-name.onrender.com
   ```
3. Перезапустите деплой в Vercel (Redeploy)

---

## 🧪 Проверка работы

### Проверьте бэкенд:
Откройте в браузере: `https://your-app-name.onrender.com/api`
Должно показать: `{"message": "API is running", "version": "1.0-simple"}`

### Проверьте документацию API:
Откройте: `https://your-app-name.onrender.com/docs`

### Проверьте фронтенд:
Откройте: `https://your-project.vercel.app`
Должен загрузиться сайт

---

## 📝 Важные замечания

### Данные в памяти:
⚠️ Сейчас данные хранятся в памяти сервера - при перезапуске они пропадут!
- Для постоянного хранения нужна БД (PostgreSQL/MongoDB)
- На бесплатном Render сервис "засыпает" через 15 минут неактивности

### CORS:
Если возникают ошибки CORS:
1. В `server_simple.py` уже настроено разрешение всех доменов (`allow_origins=["*"]`)
2. Это работает для тестирования, но для продакшена лучше указать конкретные домены

### Переменные окружения:
- **Фронтенд**: `VITE_BACKEND_URL` (в Vercel) - ⚠️ **ОБЯЗАТЕЛЬНО** установите!
  - Установите URL вашего Render сервиса: `https://your-app-name.onrender.com`
  - Без этой переменной приложение может не работать в продакшене
- **Бэкенд**: `PORT` устанавливается автоматически Render'ом

---

## 🔄 Обновление приложения

После изменений в коде:
1. Сделайте `git push` в GitHub
2. Render и Vercel автоматически перезапустят деплой
3. Подождите ~2-3 минуты

---

## 🆘 Проблемы?

### Ошибка компиляции Rust / "metadata-generation-failed":
**Проблема**: Старые версии pydantic требуют компиляции Rust, что не работает на Render
**Решение**: 
- ✅ Обновлен `requirements.txt` на новые версии (уже сделано)
- Сделайте `git commit` и `git push` изменений
- Render автоматически перезапустит деплой с новыми версиями

### Ошибка "must match re" при создании сервиса:
- ❌ **Неправильно**: Вы ввели команды в поле **Included Paths**
- ✅ **Правильно**: 
  - Команды идут в **Build Command** и **Start Command** (вверху формы)
  - **Included Paths** - только для путей к файлам (`requirements.txt`, `server_simple.py`, `src/**`) или оставьте пустым
  - Удалите команды из **Included Paths** и оставьте это поле пустым

### Бэкенд не отвечает:
- Проверьте логи в Render Dashboard
- Убедитесь что `Start Command` правильный: `uvicorn server_simple:app --host 0.0.0.0 --port $PORT`

### Фронтенд не подключается:
- Проверьте `VITE_BACKEND_URL` в Vercel
- Убедитесь что бэкенд работает (проверьте `/api` endpoint)

### CORS ошибки:
- Откройте консоль браузера (F12)
- Проверьте URL в ошибке - он должен совпадать с `VITE_BACKEND_URL`

---

## 🎉 Готово!

Теперь ваше приложение доступно в интернете! 🚀
