# 📱 Доступ с мобильного телефона

## ✅ Что было исправлено:

1. **Vite настроен** для доступа с других устройств (`host: '0.0.0.0'`)
2. **Бэкенд уже слушает** на `0.0.0.0:8000`

---

## 🚀 Как подключиться с телефона:

### Ваш локальный IP адрес: `192.168.1.250`

### Шаг 1: Запустите бэкенд
В терминале:
```bash
cd "/Users/antonkoshelev/Documents/Web-разработка/polina site"
python3 server_simple.py
```

Должно быть:
```
🚀 Запуск упрощенного сервера (данные в памяти)
📊 API доступен на: http://0.0.0.0:8000
```

### Шаг 2: Запустите фронтенд
В другом терминале:
```bash
cd "/Users/antonkoshelev/Documents/Web-разработка/polina site"
npm run dev
```

Теперь Vite покажет:
```
➜  Local:   http://localhost:3000/
➜  Network: http://192.168.1.250:3000/
```

### Шаг 3: Обновите .env файл
```bash
echo "VITE_BACKEND_URL=http://192.168.1.250:8000" > .env
```

**⚠️ Важно**: После изменения .env нужно **перезапустить** `npm run dev`

### Шаг 4: Подключитесь с телефона

1. **Убедитесь что телефон в той же WiFi сети**, что и компьютер
2. Откройте браузер на телефоне
3. Введите адрес: **`http://192.168.1.250:3000`**

✅ Готово! Сайт должен открыться на телефоне

---

## 🔧 Проверка подключения:

### Проверьте бэкенд на телефоне:
Откройте в браузере телефона: `http://192.168.1.250:8000/api`

Должно показать:
```json
{"message": "API is running", "version": "1.0-simple"}
```

### Если не работает:

1. **Проверьте брандмауэр:**
   ```bash
   # На Mac
   sudo /usr/libexec/ApplicationFirewall/socketfilterfw --getglobalstate
   ```
   
   Если включен, разрешите доступ:
   ```bash
   # Разрешить Python
   sudo /usr/libexec/ApplicationFirewall/socketfilterfw --add /usr/bin/python3
   sudo /usr/libexec/ApplicationFirewall/socketfilterfw --unblockapp /usr/bin/python3
   ```

2. **Проверьте что устройства в одной сети:**
   - Компьютер и телефон должны быть подключены к одной WiFi сети
   - Проверьте IP адрес компьютера:
     ```bash
     ifconfig | grep "inet " | grep -v 127.0.0.1
     ```

3. **Проверьте что порты открыты:**
   ```bash
   # Проверка что порты слушаются
   lsof -i :3000  # Фронтенд
   lsof -i :8000  # Бэкенд
   ```

---

## 💡 Советы:

### Автоматическое определение IP:
Можно создать скрипт для автоматического обновления .env:

```bash
#!/bin/bash
IP=$(ifconfig | grep "inet " | grep -v 127.0.0.1 | awk '{print $2}' | head -1)
echo "VITE_BACKEND_URL=http://$IP:8000" > .env
echo "Updated .env with IP: $IP"
```

Сохраните как `update-ip.sh`, сделайте исполняемым:
```bash
chmod +x update-ip.sh
./update-ip.sh
```

### Использование ngrok (альтернатива):
Если не работает через локальную сеть, можно использовать ngrok:

```bash
# Установите ngrok
brew install ngrok

# Запустите туннель для бэкенда
ngrok http 8000

# Получите публичный URL (например: https://abc123.ngrok.io)
# Обновите .env:
echo "VITE_BACKEND_URL=https://abc123.ngrok.io" > .env
```

---

## ⚠️ Важно:

- **Безопасность**: Локальный доступ работает только в вашей WiFi сети
- **IP может измениться**: Если роутер перезапустится, IP может поменяться
- **Порт 3000 и 8000**: Должны быть свободны на вашем компьютере

---

## 🎉 Готово!

Теперь вы можете тестировать приложение на реальном мобильном устройстве!
